.DS_Store
.claude/settings.local.json
node_modules/

# never commit the exported site archive
website-pellucid.zip
*.zip

# real SMTP secret — keep the key out of git (use smtp_config.example.php as the template)
smtp_config.php
