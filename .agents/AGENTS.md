# Agent Guidelines & Behavioral Constraints

## Testing Rules
- **No Automatic Browser Testing:** Do not invoke browser subagents or perform browser-based screenshot/DOM testing. The user will perform all testing and verification manually. Only run browser testing tools if the user explicitly asks you to do so in a prompt.
